[package]
name = "void-network"
version = "0.1.0"
edition = "2024"

[dependencies]
void-core = { path = "../core" }

# Async runtime (используется только RateLimiter: tokio::sync::Mutex)
tokio = { workspace = true }

# Логирование
tracing = { workspace = true }