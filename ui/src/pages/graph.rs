use eframe::egui;

pub struct Graph {
    
}

impl Default for Graph {
    fn default() -> Self {
        Self {
            
        }
    }
}

impl Graph {
    pub fn show(&mut self, ui: &mut egui::Ui) {

    }
}
